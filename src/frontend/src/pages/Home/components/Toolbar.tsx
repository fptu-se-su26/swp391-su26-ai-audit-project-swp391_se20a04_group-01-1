import React from 'react';

/**
 * Toolbar
 * Component này CHƯA tồn tại trong code gốc bạn gửi (Home.tsx cũ không có thanh công cụ trên cùng).
 * Đây là khung tối giản để khớp với cấu trúc Home.tsx mục tiêu bạn đề xuất - bạn cần bổ sung
 * nội dung thực tế (logo, menu, tài khoản...) theo thiết kế của dự án.
 */
export const Toolbar: React.FC = () => {
  return (
    <div className="absolute top-0 left-0 right-0 z-30 h-14 flex items-center px-6 pointer-events-none">
      {/* TODO: nội dung thanh công cụ */}
    </div>
  );
};
